// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ChoiceOption } from "./ChoiceOption";
import type { NumericRange } from "./NumericRange";
import type { QuestionType } from "./QuestionType";
import type { Scale } from "./Scale";

/**
 * The part of a question a reviewer edits. Which fields apply depends on the type:
 * options (choice types), scale (likert), numeric (numeric).
 */
export type QuestionBody = { text: string, questionType: QuestionType, options: Array<ChoiceOption>, 
/**
 * Shuffle option order per respondent (choice types).
 */
randomize: boolean, 
/**
 * Most options a respondent may pick (multi choice).
 */
maxChoices: number | null, scale: Scale | null, numeric: NumericRange | null, };
