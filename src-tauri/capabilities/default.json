{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may only use core events and this app's own commands. No shell, fs, http or sql plugins. The export save dialog is opened from Rust (export_run), so the window needs no dialog permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
